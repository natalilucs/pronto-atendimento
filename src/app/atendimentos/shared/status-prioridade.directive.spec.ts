import { StatusPrioridadeDirective } from './status-prioridade.directive';

describe('StatusPrioridadeDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusPrioridadeDirective();
    expect(directive).toBeTruthy();
  });
});
