import { AtendimentoFinalizadoDirective } from './atendimento-finalizado.directive';

describe('AtendimentoFinalizadoDirective', () => {
  it('should create an instance', () => {
    const directive = new AtendimentoFinalizadoDirective();
    expect(directive).toBeTruthy();
  });
});
